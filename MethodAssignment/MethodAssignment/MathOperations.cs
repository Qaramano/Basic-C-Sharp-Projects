using System;

public class MathOperations
{
    // Method to perform addition (you can change the operation as needed)
    public int PerformMathOperation(int num1, int num2 = 0)
    {
        int result = num1 + num2;
        return result;
    }
}
